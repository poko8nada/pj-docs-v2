# agenda — Goal / Discover

Use when this `/work` session advances **Goal** and/or **Discover** (axes, softs, cheap media). Soft skills are normal here. Opening `[Build]` when Goal + Discover are agreed also belongs here.

Build-stage product work → `build.md` (softs are still allowed there).

Slice table and fills → parent `agenda/SKILL.md`.

## Inventory sources

- Open `[Goal]` / `[Discover]` bodies (overview + why + agreed)
- Soft comments (`## soft: …`) and cited `findings/` paths
- Session scope agreed in discussion

## What this situation often does

- Run softs (`feasibility`, `foundation`, `grain`, `inventory`, `data-model`) → write `findings/` → update the one soft comment via `issue`
- Cheap media only while Discovering (look workshop / spike / disposable HTML) — inventing Goal by rebuilding the product tree is out
- Update Goal / Discover axes (and agreed) when the slice’s outcome warrants it
- Create `[Build]` with Links + thin Roadmap/Test/Deploy when Goal + Discover are agreed and the user wants product work

Harness / meta → `/chore`.

## After a slice

Commit when agreed (per slice or batched). Update issues at milestones or when the deliverable is an axis/soft comment change. If direction stalls, name `/discussion`.
